import { describe, it, expect } from "@jest/globals";

// 仅测试纯函数逻辑，不测试外部 API 调用
// 外部 API 测试需要 mock fetch
describe("词典 API 模块", () => {
  it("placeholder - dictionary API tests would require mocking fetch", () => {
    expect(true).toBe(true);
  });
});
